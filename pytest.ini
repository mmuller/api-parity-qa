[pytest]
testpaths = tests

markers =
    happy_path: clean flow
    negative: invalid inputs
    bug: failure detection
    parity: tests that validate system parity between implementations
    contract: API contract validation
